# mediapipe currently only supports Python 3.9-3.12 (not 3.13/3.14).
# If your venv is on a newer Python, create a separate one, e.g.:
#   python3.12 -m venv venv312 && source venv312/bin/activate
#   pip install -r requirements.txt
opencv-python
mediapipe
pyserial
